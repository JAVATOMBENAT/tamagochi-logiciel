package com.example.tamagochi;

import javafx.fxml.FXML;
import javafx.scene.control.Button;
import javafx.scene.control.Label;
import javafx.scene.control.ListView;
import javafx.scene.control.TextField;

import java.io.IOException;

public class PrimaryController {

    // @FXML
    // private ListView<String> taskListView;

    // @FXML
    // private TextField taskField;

    // @FXML
    // private Label pointderecompense;

    // @FXML
    // private void addTask() {
    //     String task = taskField.getText().trim();
    //     if (!task.isEmpty()) {
    //         taskListView.getItems().add(task);
    //         taskField.clear();
    //     }
    // }

    // @FXML
    // private void editTask() {
    //     int index = taskListView.getSelectionModel().getSelectedIndex();
    //     if (index >= 0) {
    //         String newTask = taskField.getText().trim();
    //         if (!newTask.isEmpty()) {
    //             taskListView.getItems().set(index, newTask);
    //             taskField.clear();
    //         }
    //     }
    // }

    // @FXML
    // private void deleteTask() {
    //     int index = taskListView.getSelectionModel().getSelectedIndex();
    //     if (index >= 0) {
    //         taskListView.getItems().remove(index);
    //     }
    // }

    // @FXML
    // private void validateTask() {
    //     int index = taskListView.getSelectionModel().getSelectedIndex();
    //     if (index >= 0) {
    //         taskListView.getItems().remove(index);
    //         // Ajoutez ici la logique pour incrémenter les points
    //         // et mettre à jour le label "pointderecompense"
    //     }
    // }

    // @FXML
    // private void returnToMenu() throws IOException {
    //     App.setRoot("menu");
    // }

}